import Link from 'next/link'; import {services} from '@/lib/data'; export const metadata={title:'Digital Marketing Services'};
export default function Page(){return <main><section className="page-hero"><div className="shell"><div className="kicker">Services</div><h1>Specialist delivery.<br/>One commercial plan.</h1><p className="lead">Choose a focused engagement or combine services around a larger growth objective.</p></div></section><section className="section"><div className="shell service-grid">{services.map((s,i)=><Link href={'/services/'+s.slug} className="service-card" key={s.slug}><span>0{i+1}</span><div><small>{s.eyebrow}</small><h3>{s.name}</h3><p>{s.summary}</p><b>{s.from} · Explore →</b></div></Link>)}</div></section></main>}
