import {notFound} from 'next/navigation'; import Link from 'next/link'; import {services} from '@/lib/data';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}){const {slug}=await params; const s=services.find(x=>x.slug===slug); return s?{title:s.name,description:s.summary,alternates:{canonical:`/services/${s.slug}`}}:{} }
export default async function Page({params}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();return <main><section className="page-hero"><div className="shell"><div className="kicker">{s.eyebrow}</div><h1>{s.name}</h1><p className="lead">{s.desc}</p><div className="actions"><Link href="/contact" className="btn">Discuss your project</Link><span className="price">{s.from}</span></div></div></section><section className="section"><div className="shell split"><div><div className="kicker">What’s included</div><h2>Built around the work that moves the outcome.</h2></div><div className="deliverables">{s.deliverables.map((x,i)=><div key={x}><b>{x}</b></div>)}</div></div></section><section className="soft section"><div className="shell"><div className="kicker">Our process</div><div className="process"><div><b>Discover</b><p>Understand the business, customer, baseline and target.</p></div><div><b>Prioritise</b><p>Choose the highest-value work and define success measures.</p></div><div><b>Deliver</b><p>Execute in short cycles with clear ownership and communication.</p></div><div><b>Improve</b><p>Measure performance, learn and reinvest in what works.</p></div></div></div></section></main>}
